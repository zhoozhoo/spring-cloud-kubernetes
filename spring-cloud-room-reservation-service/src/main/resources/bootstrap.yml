spring:
  application:
    name: room-reservation-service
  cloud:
    config:
      uri: http://localhost:8888
    loadbalancer:
      ribbon:
        enabled: false
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
